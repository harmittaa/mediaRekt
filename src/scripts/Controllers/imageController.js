// IMAGE VIEW CONTROLLER

mediaRekt.controller("ImageController", function ($scope) {
    console.log(window.location.href);
    var urlParam = window.location.href.split("=");
    var contentId = urlParam[1];
    console.log(contentId);
    
                                               
    
});